<!DOCTYPE html>
<html lang="en">

  <head>

    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100&display=swap" rel="stylesheet">

    <title>Philosophy Cloud</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%# cloudinary_js_config %>

  </head>

  <body>

  <div>

    <nav>

        <div class="logo">
          <h3><%= link_to "Philosophy Cloud", root_path, :class => "logo-btn" %></h3>
        </div>


        <div>

            <% if @current_user.present? %>
            
            <%= link_to "Materials", materials_path, :class => "btn"%>
            <%= link_to "Topics", topics_path, :class => "btn"%>
            <%= link_to "Philosophers", philosophers_path, :class => "btn"%>
            <%= link_to "Sign out #{ @current_user.email }", login_path, method: 'delete', :class => "btn"%>
    
          <% else%>
            
            <%= link_to "Sign up", new_user_path, :class => "btn"%>
            <%= link_to "Sign in", login_path, :class => "btn"%>

          <% end %>

        </div>

        <div class="nav-button">
          <div class="line1"></div>
          <div class="line2"></div>
          <div class="line3"></div>
        
        </div>
    
    </nav>

    <p class="notice"><%= flash[:notice] %></p>
    <p class="alert"><%= flash[:alert] %></p>

  <%= yield %>
  <%= javascript_include_tag 'application' %>

  </body>

</html>
